<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StreetEye_App.Views.Usuarios.LoginView"
             Title="LoginView"
             Background="WhiteSmoke">
    <ScrollView>
        <StackLayout>
            <Label Text="StreetEye" FontSize="Large" HorizontalOptions="Center" />
            <Entry Placeholder="Email" Text="{Binding Email}" />
            <Entry Placeholder="Senha" Text="{Binding Password}" IsPassword="True" />
            <Button Text="Entrar" Command="{Binding LoginCommand}" />
        </StackLayout>
    </ScrollView>
</ContentPage>